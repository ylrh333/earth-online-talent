# Optional local defaults. The app also supports entering these values in the UI.
# Do not commit real API keys.

EARTH_ONLINE_DEFAULT_BASE_URL=https://api.openai.com/v1
EARTH_ONLINE_DEFAULT_MODEL=gpt-4o-mini
